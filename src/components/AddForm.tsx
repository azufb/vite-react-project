const AddForm = () => {
    return (
        <form>
            <label htmlFor="task">タスク：</label>
            <input id="task" />
            <button>ADD</button>
        </form>
    );
};

export default AddForm;